{
  "skill_name": "pr-teacher",
  "evals": [
    {
      "id": 0,
      "name": "ca-bundle-incident",
      "prompt": "teach me PR 1875 in Intellilake-Platform/platform — I need to understand the CA bundle thing well enough to explain it to the team on Monday",
      "expected_output": "Self-contained HTML artifact teaching PR #1875. Should identify the spine (undici's `ca` option replaces Node's trust store rather than extending it, so 6 corporate certs silently dropped 144 Mozilla roots), map all 10 changed files including the 4 new ones, and include an operable element — most naturally a trust-store composition toggle.",
      "files": []
    },
    {
      "id": 1,
      "name": "redos-regex",
      "prompt": "can you help me understand https://github.com/Intellilake-Platform/platform/pull/1872 — I'm reviewing some similar regex code and I want to actually get why the old pattern was slow, not just be told it was",
      "expected_output": "Self-contained HTML artifact teaching PR #1872. Should identify the spine (the outer `*` wraps a character class that already contains `/`, so the matcher has exponentially many ways to split the same string), include a live regex input or complexity demo that actually runs both patterns, and surface the unresolved reviewer concern about the n=100 test hanging CI rather than failing.",
      "files": []
    },
    {
      "id": 2,
      "name": "asyncio-lock-race",
      "prompt": "walk me through PR 1874, I have to answer questions about it",
      "expected_output": "Self-contained HTML artifact teaching PR #1874. Should identify the spine (asyncio.Lock only binds to a loop on its contended acquire path, so an orphaned waiter poisons the cached lock for every later caller), include a steppable timeline or interleaving explorer, and report honestly that the PR merged with changes still requested about the except-RuntimeError scope.",
      "files": []
    }
  ]
}
